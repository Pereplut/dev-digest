import { SkillsIndex } from "./_components/SkillsIndex/SkillsIndex";

/* Route: /skills — opens the first skill, or shows the create/import empty state. */
export default function SkillsPage() {
  return <SkillsIndex />;
}
