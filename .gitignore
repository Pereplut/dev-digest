node_modules/
dist/
# agent-runner ships as a JS GitHub Action — its bundled dist/ MUST be committed
# (GitHub runs action.yml `main: dist/index.js` as-is, with no build step).
!agent-runner/dist/
!agent-runner/dist/**
build/
.next/
.next-e2e/
out/
coverage/
*.tsbuildinfo
.env
.env.local
.DS_Store
.idea/
.turbo/
.pnpm-store/
*.log
__pycache__/
.devdigest/cache/
clones/
playwright-report/
test-results/

# engineering-insights hook state (condensed transcripts, per-session markers)
.claude/.insights-state/

# pr-self-review state (plan, findings, verdict, report) — per-clone, per-diff
.claude/.pr-self-review/

# emdash worktree config (local)
.emdash.json
